[project]
name = "ctrl-c-club"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = [
    "requests",
    "squiral",
]

[dependency-groups]
dev = [
    "pre-commit",
    "pytest",
]

[tool.uv]
package = false
